scikit-optimize
configspace
smac==0.13.1

# Below are requirements for special implementations of Bayesian optimization
# Probably you do not need them but if so then uncomment the following lines:
# bayesmark
# GPy
# GPyOpt
